
export default function optimizeAst (args) {
// See if this is a '//XXX' shorthand, this would be compiled to /descendant-or-self::node()/child::XXX, where 'descendant::XXX' would be enough
	if (args.some((pathPart, i) => {
		if (pathPart[0] === 'child') {
			return
		}
	})) {

	}
};
