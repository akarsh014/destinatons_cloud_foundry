import builtinDataTypesByName from '../builtins/builtinDataTypesByName';

export default function instanceOfType (value, type) {
	if (value.type === type) {
		return true;
	}

	return builtinDataTypesByName[value.type].instanceOfType(type);
}
